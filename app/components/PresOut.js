import React from 'react';
import CustomPaginationActionsTable from './ui_elements/CustomPaginationActionsTable';

const PresOut = (props) => (
  <div>
    <CustomPaginationActionsTable {...props} />
  </div>
);

export default PresOut;
